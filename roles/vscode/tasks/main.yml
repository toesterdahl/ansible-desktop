---
# First export key using apt-key
# apt-key list
# apt-key export <key id> > keys/microsoft-prod.gpg
# Where id is the alpha-numerical id in the list output..
#
- name: VsCode - Add VsCode's official GPG Key
  become: true
  copy: 
    src: "{{ role_path }}/defaults/microsoft-vscode.gpg"
    dest: /etc/apt/keyrings/microsoft-vscode.gpg
    mode: '0644'
    owner: root
    group: root

- name: VsCode - Setup VsCode stable repository
  become: true
  deb822_repository:
    name: vscode
    types: deb
    uris: https://packages.microsoft.com/repos/code
    suites: stable
    components: main
    architectures: amd64
    signed_by: /etc/apt/keyrings/microsoft-vscode.gpg

- name: Update apt cache
  become: true
  apt: update_cache=yes force_apt_get=yes cache_valid_time=3600

- name: VsCode - Install
  become: true
  apt:
    name: "{{ item }}"
    state: latest
  loop:
    - code

